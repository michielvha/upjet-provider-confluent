apiVersion: schema.confluent.crossplane.io/v1alpha1
kind: RegistryClusterMode
metadata:
  annotations:
    meta.upbound.io/example-id: schema/v1alpha1/registryclustermode
  labels:
    testing.upbound.io/example-name: example
  name: example
spec:
  forProvider:
    credentials:
    - keySecretRef:
        key: example-key
        name: example-secret
        namespace: upbound-system
      secretSecretRef:
        key: example-key
        name: example-secret
        namespace: upbound-system
    mode: READONLY
    restEndpoint: ${data.confluent_schema_registry_cluster.essentials.rest_endpoint}
    schemaRegistryCluster:
    - id: ${data.confluent_schema_registry_cluster.essentials.id}
