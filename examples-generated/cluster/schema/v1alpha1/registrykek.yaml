apiVersion: schema.confluent.crossplane.io/v1alpha1
kind: RegistryKek
metadata:
  annotations:
    meta.upbound.io/example-id: schema/v1alpha1/registrykek
  labels:
    testing.upbound.io/example-name: aws_key
  name: aws-key
spec:
  forProvider:
    credentials:
    - keySecretRef:
        key: example-key
        name: example-secret
        namespace: upbound-system
      secretSecretRef:
        key: example-key
        name: example-secret
        namespace: upbound-system
    doc: test key
    hardDelete: true
    kmsKeyId: key_id
    kmsType: aws-kms
    restEndpoint: ${data.confluent_schema_registry_cluster.essentials.rest_endpoint}
    schemaRegistryCluster:
    - id: ${data.confluent_schema_registry_cluster.essentials.id}
    shared: false
